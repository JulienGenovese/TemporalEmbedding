[project]
name = "embeddingclient"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.2",
    "torch>=2.11.0",
    "transformers>=5.5.3",
]
